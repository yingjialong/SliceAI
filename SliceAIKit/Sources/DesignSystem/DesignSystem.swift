import Foundation

/// DesignSystem 模块命名空间占位
///
/// 本模块集中管理所有视觉 tokens（colors / typography / spacing / radius / shadow /
/// animation / material）、通用 Components 与 Modifiers，供 Windowing / SettingsUI /
/// Permissions 等 UI 层消费。本文件仅作为模块命名空间占位，无任何运行时逻辑。
public enum DesignSystem {
    /// 模块版本号，随设计语言大版本同步
    public static let version = "1.0"
}
