import Foundation

/// **Task 3 STUB** — full implementation in Task 13 (M2.9 MCPClientProtocol + SkillRegistryProtocol).
///
/// Empty body so `ExecutionEngine` can use `any MCPClientProtocol` in its init.
/// Task 13 will add `func call(server:tool:args:) async throws -> MCPCallResult`
/// and supporting types.
public protocol MCPClientProtocol: Sendable {}
