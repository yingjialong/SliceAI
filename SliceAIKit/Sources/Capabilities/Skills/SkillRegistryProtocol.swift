import Foundation

/// **Task 3 STUB** — full implementation in Task 13 (M2.9 MCPClientProtocol + SkillRegistryProtocol).
///
/// Empty body so `ExecutionEngine` can use `any SkillRegistryProtocol` in its init.
/// Task 13 will add `func resolve(_ ref: SkillReference) async throws -> ResolvedSkill`
/// and supporting types.
public protocol SkillRegistryProtocol: Sendable {}
