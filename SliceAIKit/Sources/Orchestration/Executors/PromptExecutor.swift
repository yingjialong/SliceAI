import Foundation

/// **Task 3 STUB** — full implementation in Task 11 (M2.7 PromptExecutor actor).
///
/// Only `init()` is provided so `ExecutionEngine` can construct the type at Task 3 wiring stage.
/// Task 11 will add `init(keychain:)` and
/// `stream(tool:context:provider:) async throws -> AsyncThrowingStream<PromptStreamElement, Error>`.
public actor PromptExecutor {
    /// Task 3 stub init — Task 11 will replace with `init(keychain: any KeychainAccessing)`.
    public init() {}
}
