import Foundation

/// **Task 3 STUB** — full implementation in Task 7 (M2.4 EffectivePermissions + PermissionGraph).
///
/// Only `init()` is provided so `ExecutionEngine` can construct the type at Task 3 wiring stage.
/// Task 7 will add `func compute(declared:sideEffects:toolCalls:) -> EffectivePermissions`
/// and the `EffectivePermissions` aggregate type.
public actor PermissionGraph {
    /// Task 3 stub init — Task 7 will replace with a richer initializer accepting policy config.
    public init() {}
}
