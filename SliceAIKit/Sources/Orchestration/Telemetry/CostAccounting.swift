import Foundation

/// **Task 3 STUB** — full implementation in Task 8 (M2.5 CostAccounting actor + SQLite append).
///
/// Only `init()` is provided so `ExecutionEngine` can construct the type at Task 3 wiring stage.
/// Task 8 will add `init(dbURL:)` and `func append(_ record: CostRecord) async throws`
/// along with the `CostRecord` struct.
public actor CostAccounting {
    /// Task 3 stub init — Task 8 will replace with `init(dbURL: URL)`.
    public init() {}
}
