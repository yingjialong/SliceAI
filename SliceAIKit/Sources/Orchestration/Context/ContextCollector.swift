import Foundation

/// **Task 3 STUB** — full implementation in Task 5 (M2.2 ContextCollector 平铺并发实现).
///
/// Only `init()` is provided so `ExecutionEngine` can construct the type at Task 3 wiring stage.
/// Task 5 will add `init(registry: ContextProviderRegistry)` and
/// `resolve(seed:requests:) async throws -> ResolvedExecutionContext`.
public actor ContextCollector {
    /// Task 3 stub init — Task 5 will replace with `init(registry: ContextProviderRegistry)`.
    public init() {}
}
