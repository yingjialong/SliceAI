# v0.3 Release Prep

## 任务背景

Phase 1 MCP + Context 主干已经合并到 `main`，真实 5-server MCP 直接 E2E 和用户 App 场景基本复测均未反馈阻塞问题。当前任务不是继续新增功能，而是在发布 `v0.3` 前把最终 gate、review loop、release notes 和 tag 准备状态收口，避免把“功能基本可用”误判为“可以发布”。

## 实施方案

1. 在 `main` 上记录 release prep 任务和项目状态。
2. 确认所有 feature / docs worktree 已合并或清理，记录仍保留的归档分支边界。
3. 运行最终 release gate，覆盖 SwiftPM 全量测试、SwiftLint strict、whitespace check 和 App Debug build。
4. 对 `main` 相对 `origin/main` 的本地待发布变更运行一次 Claude review loop；本轮是 broad release pass，接受 diff 很大的事实，但只处理 material findings。
5. 若 review 发现必须修复的问题，按根因做最小修复并复跑相关验证；若通过，则更新 release notes / tag checklist。
6. 在用户确认发布动作前，不推送远端、不创建 GitHub Release。

## ToDoList

- [x] 创建本任务文档。
- [x] 登记 `docs/Task_history.md`。
- [x] 确认 `main` 工作树干净且领先 `origin/main`。
- [x] 确认历史 worktree / feature 分支已合并或清理。
- [x] 记录最终 gate 的当前结果。
- [x] 运行最终 Claude review loop。
- [x] 处理 review findings 或记录 approve。
- [x] 更新 README、master TodoList 和 Task 17 状态。
- [x] 准备 `v0.3` release notes / tag checklist。
- [x] 提交 release prep 文档。

## 当前状态

- 当前分支：`main`。
- 本任务修改前基线：`afde622`（merge: phase 0 m3 review artifacts）。
- 当前 `main` 包含本任务的 release prep 代码修复与文档改动；`v0.3.0` tag 已推送，GitHub Actions Release run `26168050987` 已成功生成 draft release，等待人工发布。
- 已合并 / 清理：`feature/phase-1-mcp-context`、`docs/v0.2.0-release-published`、`archive/phase-0-m3-review-loop-artifacts`、`docs/pre-phase1-closeout`、`feature/phase-0-m3-switch-to-v2`。
- 保留归档分支：`archive/pre-phase1-local-appcontainer-snapshot`。该分支只保存 `SliceAIApp/AppContainer.swift` 的本机旧快照，不是发布分支，不应静默合并。
- 本地 `main` 尚未 push 到远端，发布前仍需用户确认远端 push / tag / GitHub Release。

## Claude Review Loop 结果

- 记录文件：`docs/Task-detail/claude-loop-v0.3-release-prep.md`。
- Round 1 verdict：`needs_attention`，0 critical / 1 high / 1 medium / 0 low。
- Round 2 verdict：`approve`，`findings: []`。

接受并修复：

1. **High**：长 MCP tool result 经过 `Redaction.scrub` 后变成 `<truncated:N>`，该短摘要又被回填给 LLM 的 `.tool` role message，导致 `web-search-summarize` 无法看到真实 Brave 搜索结果。修复后 UI / 日志仍使用短摘要，LLM tool message 使用 pattern-only 脱敏并保留最多 16 KiB 前缀内容。
2. **Medium**：`StdioMCPClient` 按 descriptor id 缓存旧进程，Settings 修改 command / args / env 后旧 runner 会继续运行到 idle timeout 或 App 重启。修复后 stdio session 会比较 transport / command / args / env / url，启动配置变化时 teardown 并重启。

## 代码修改逻辑

1. `Redaction.scrub(_:)` 仍保持 audit / UI 语义：先替换敏感模式，再用 `<truncated:N>` 限制短字段体积。
2. 新增 `Redaction.scrubSecrets(_:)`：只替换 Bearer、`sk-`、Authorization、Cookie 等敏感模式，不做长度截断，供模型可见的工具结果使用。
3. `AgentExecutor` 将 MCP result 分成两条路径：
   - `summarize(result:)`：给 ResultPanel / error row 的短摘要，仍最多 200 字符。
   - `toolMessageContent(result:)`：给下一轮 LLM 的 `.tool` role content，脱敏后保留最多 16 KiB 前缀，超出时追加 `<truncated:N>`。
4. `AgentToolCallRunState.recordToolMessageContent` 现在读取保留内容后的 tool message，因此长 rate-limit body 仍能命中 `stopOnRateLimit`。
5. `StdioMCPProcessSession.matchesLaunchConfiguration(of:)` 显式比较启动输入，绕开 `MCPDescriptor ==` 的 id-only 身份语义；`StdioMCPClient.session(for:)` 在 start-in-flight 和 cached session 两个分支都能识别 stale session 并重启。

## 最终 Gate 结果

合并到 `main` 后已经完成一次最终 gate：

- 已通过：`swift test --package-path SliceAIKit`（756 tests，0 failures）。
- 已通过：`swiftlint lint --strict`（172 files，0 violations）。
- 已通过：`git diff --check HEAD~1 HEAD && git diff --check`。
- 已通过：`xcodebuild -project SliceAI.xcodeproj -scheme SliceAI -configuration Debug build`。

已知非阻塞警告：

- Swift 编译警告：`MCPServerStoreTests.swift:196` 对 `update` 返回值未使用；当前不影响测试结果，可作为后续清理项。
- Xcode build 警告：多 matching destinations、AppIntents metadata extraction skipped、Debug ad-hoc codesigning 下 hardened runtime disabled；均为本地 Debug 构建常见警告，不是 release blocker。

Review fix 后复跑：

- 已通过：`swift test --package-path SliceAIKit --filter 'AgentExecutorTests|StdioMCPClientTests|RedactionTests'`（55 tests）。
- 已通过：`swift test --package-path SliceAIKit`（758 tests；第一次出现 1 个未复现取消竞态瞬时失败，随后单测与全量复跑均通过）。
- 已通过：`swiftlint lint --strict`（172 files，0 violations）。
- 已通过：`git diff --check HEAD~1 HEAD && git diff --check`。
- 已通过：`xcodebuild -project SliceAI.xcodeproj -scheme SliceAI -configuration Debug build`。
- 已通过：`scripts/build-dmg.sh 0.3.0`。
- 已通过：`shasum -a 256 build/SliceAI-0.3.0.dmg`，结果为 `e2c111a0c6cbfe8f460a63ff92079be0abdb5ed629f2db2ca048c2fbe1a8b5ca`。
- 已通过：DMG 只读挂载结构校验，包含 `SliceAI.app` 和 `Applications -> /Applications`。

Release workflow 首次触发后修复：

- 2026-05-20 已执行 `git push origin main` 并推送 `v0.3.0` tag，GitHub Actions Release run `26167656542` 被触发。
- 首次 run 在 `Build DMG` / Xcode 16.4 Release archive 阶段失败，核心错误是 `StreamableHTTPMCPClient.retryingExpiredSession<Result>` 的泛型 `Result` 未约束为 `Sendable`。
- 根因：本地 Debug build 未覆盖 CI Release archive 的 Swift 6 严格并发路径；该 helper 跨 actor / async 边界返回泛型值，CI 编译器要求结果类型可发送。
- 修复：将 `retryingExpiredSession` 改为 `Result: Sendable`。实际调用返回 `[MCPToolDescriptor]` 与 `MCPCallResult`，二者均已声明 `Sendable`，因此是类型约束补齐，不改变运行逻辑。
- 已通过：`swift test --package-path SliceAIKit --filter 'StreamableHTTPMCPClientTests|RoutingMCPClientTests'`（12 tests）。
- 已通过：`swiftlint lint --strict`（172 files，0 violations）。
- 已通过：`scripts/build-dmg.sh 0.3.0`。
- 已通过：`shasum -a 256 build/SliceAI-0.3.0.dmg`，最新本地预检结果为 `1520d53e6e0edd097c30f6d6552f28d8b0bc0f80799e0b080f0b36a2bd121e34`。
- 已通过：重指向并推送 `v0.3.0` tag 后，GitHub Actions Release run `26168050987` 完成成功。
- 已通过：draft release artifact `SliceAI-0.3.0.dmg` 文件名正确，release body SHA 和下载后本地 `shasum -a 256` 均为 `cf63e4e50b8eeda63e38f04c85ff485d11cdfa939038d7555b72ae61ad96f0e0`。

## v0.3 Release Notes 草稿

```markdown
## SliceAI 0.3.0

**Unsigned DMG.** Installation steps:
1. Mount the DMG and drag `SliceAI.app` to `/Applications`
2. First launch: Right-click -> Open (because unsigned)
   Or run: `xattr -d com.apple.quarantine /Applications/SliceAI.app`
3. Grant Accessibility permission when prompted
4. Configure an OpenAI-compatible provider and optional MCP servers

### Highlights
- MCP support: stdio and Streamable HTTP clients, Claude Desktop `mcp.json` import, MCP Servers Settings page, connection diagnostics, and safe redaction boundaries.
- Context support: selection, front app title, front app URL, clipboard text, and sandboxed file reads.
- Agent tools: first-party `web-search-summarize`, real AgentExecutor tool calling, ResultPanel tool-call lifecycle, permission approval / denial UI, and foundational custom Agent Tool configuration.
- Controls: per-tool hotkeys and Agent MCP allowlist / tool-call policy controls.

### Fixes from release prep
- Long MCP tool results are preserved for LLM follow-up instead of being replaced with `<truncated:N>`.
- Stdio MCP servers restart when Settings changes their command / args / env.

### Known notes
- Brave Search support currently relies on the external `@modelcontextprotocol/server-brave-search` package, which is marked deprecated upstream.
- App E2E was user-verified as basically working, but no per-scenario screenshots / logs were archived.

### Checksum
SHA256: `<filled by release workflow>`
```

## 发布 Checklist

- [x] `main` 包含 Phase 1 MCP + Context 主干。
- [x] 最终 Claude review loop Round 2 approve。
- [x] 最终 SwiftPM / SwiftLint / whitespace / Xcode Debug build gate 通过。
- [x] 本地 unsigned DMG 预检和挂载结构校验通过。
- [x] 用户确认后执行 `git push origin main`。
- [x] 用户确认后执行 `git tag v0.3.0 && git push origin v0.3.0`。
- [x] 首次 GitHub Actions Release run 触发成功，但 CI Release archive 暴露严格并发编译问题。
- [x] 修复 CI Release archive 的 `Sendable` 约束缺口，并完成本地验证。
- [x] 重推修正后的 `main` 与 `v0.3.0` tag，GitHub Actions release workflow 已生成 draft release。
- [x] 校验 draft release 的 artifact 文件名、release body SHA 和远端 DMG SHA。
- [ ] 人工发布 GitHub Release。

## 发布前风险与边界

- Brave Search MCP npm 包已标记 deprecated；当前 release 仍可用，但需要在 release notes 中标记为外部 server 依赖风险。
- App 场景复测来自用户反馈“基本测试过没有问题”，未沉淀逐项截图 / 日志证据；本次 release prep 用最终 gate 和 review loop 补齐可信度，但不伪造 E2E 截图证据。
- Skill 支持属于 Phase 2，不进入 `v0.3` 发布 blocker。
- 远端 push / tag 已完成；GitHub Release 仍保持 draft，等待人工发布。

## 当前结论

`v0.3` release prep 已完成；远端 release 首次 run 暴露并修复了 CI Release archive 的严格并发编译缺口，第二次 Release run 已成功生成 draft release。当前 go / no-go 判断为 **go for manual GitHub Release publish after human review**。
