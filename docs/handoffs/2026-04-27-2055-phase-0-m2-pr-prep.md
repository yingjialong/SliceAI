---
topic: phase-0-m2-pr-prep
title: Phase 0 M2 codex review + PR
branch: feature/phase-0-m2-orchestration
status: needs-review
created: 2026-04-27 20:55
last_updated: 2026-04-27 20:55
previous_handoff: null
---

# Phase 0 M2 codex review + PR

## Goal

Phase 0 M2（Orchestration + Capabilities 骨架）已经在 worktree `feature/phase-0-m2-orchestration` 分支完整实施完成（41 commits / 545 tests pass / coverage 全部超目标 / xcodebuild SUCCEEDED / zero-touch 三条全 PASS）。**用户明确要求**：在 push + 开 PR 之前，先用 Codex 做反复 adversarial review，直到无 issue 再开 PR。本 handoff 把"M2 已落到何种状态 + Codex review 应该聚焦什么 + 收尾 PR 流程"完整交接给下个 session。

## This session

- **Window**: 2026-04-27（前半段从压缩前的会话延续；后半段完成 Tasks 11/4/12/13/14）
- **What got done**:
  - Tasks 11 (PromptExecutor) / 4 (ExecutionEngine 主流程 Step 1-10) / 12 (PathSandbox) / 13 (MCP/Skill protocol + Mock) / 14 (集成验证 + 文档归档) 按 subagent-driven-development workflow 全部完成（implementer + spec reviewer + code quality reviewer 两阶段闭环）
  - 4 处必修 fix 已落地：① Task 12 PathSandbox `/private/etc` 硬禁止盲区（commit `de25e2b`） ② Task 13 MCPClientError.developerContext 脱敏（commit `3feacbc`） ③ Task 11 mock helper print 噪音清理（commit `e525d2d`） ④ Task 6 PermissionBrokerProtocol plan/round 元数据残留清理（commit `2fa006c`）
  - Task 14 文档归档：填完 `docs/Task-detail/2026-04-25-phase-0-m2-orchestration.md` 的 §5/§6/§7 + 在 `README.md` 项目修改变动记录追加 M2 完成段（commit `73239c3`）
  - 全套 CI gate 实测通过（数字见 §6）
- **Predecessor**: first handoff for this workstream（worktree `docs/handoffs/` 之前不存在）

> "This session" 仅是日志。M2 实施期间所有载体性决策、不变量、当前代码状态、坑都已搬运到下面各节，本文件单独自包含；下个 session 默认只读本文件 + entry docs 即可继续工作。

## Current code state

- **Branch**: `feature/phase-0-m2-orchestration`（worktree 干净，无 uncommitted）
- **HEAD**: `73239c3 docs(phase-0/m2): finalize Task-detail + README record (Task 14)`
- **Baseline**: `5cdf0f7`（M1 PR #1 merge commit）
- **Worktree path**: `/Users/majiajun/workspace/SliceAI/.worktrees/phase-0-m2/`（subdirectory worktree，根含 `CLAUDE.md` / `SliceAIApp/` / `SliceAIKit/` / `docs/` / `SliceAI.xcodeproj`）
- **主仓库**: `/Users/majiajun/workspace/SliceAI/`（HEAD = `7155ff7`，main 分支；**禁止误改**主仓库；远端 origin/main 当前 = `5cdf0f7`，主仓库 main 比远端多 2 个 docs 类 commit）

### M2 实施期间所有 commits（41 条，自 baseline 起；时间顺序倒序）

```
73239c3 docs(phase-0/m2): finalize Task-detail + README record (Task 14)
2fa006c refactor(orchestration): scrub plan/round metadata from PermissionBroker docs
3feacbc fix(capabilities): align MCPClientError redaction with SliceError convention
7b72468 feat(capabilities): add MCPClientProtocol + SkillRegistryProtocol + production Mocks (M2.9)
de25e2b fix(capabilities): close PathSandbox macOS symlink hard-deny gap
17bc63d feat(capabilities): add PathSandbox + PathSandboxError, drop Capabilities placeholder (M2.8)
b770777 feat(orchestration): wire ExecutionEngine main flow Step 1-10 (M2.1.d)
e525d2d test(orchestration): drop debug prints from PromptExecutor mock helpers
d5c9052 feat(orchestration): add PromptExecutor actor + PromptStreamElement + UsageStats (M2.7)
fd46620 refactor(orchestration): scrub plan metadata from OutputDispatcherProtocol doc
65176c0 feat(orchestration): add OutputDispatcher actor + WindowSink protocol (.window only) (M2.6)
38036f5 feat(orchestration): add AuditLogProtocol + JSONLAuditLog actor + Redaction (M2.5)
1b91d6b test(orchestration): clean up CostAccounting temp db files in ExecutionEngineTests tearDown
f8ee198 feat(orchestration): add CostAccounting actor + sqlite persistence + CostRecord (M2.4)
b30f6da feat(orchestration): add EffectivePermissions + PermissionGraph.compute D-24 closure (M2.3a)
ebbef12 feat(orchestration): add PermissionBroker + GateOutcome 4-state + §3.9.2 lower-bound matrix (M2.3)
006db0b refactor(orchestration): apply M2.2 code review fixes
196588d feat(orchestration): add ContextCollector + ContextProviderRegistry (M2.2)
9b6a473 refactor(orchestration): apply M2.1.c code review fixes
b899069 feat(orchestration): add ExecutionEngine actor skeleton + 9 stub deps + 5 Mocks (M2.1.c)
7787ce2 refactor(orchestration): apply M2.1.b code review fixes
03112c4 feat(orchestration): add ProviderResolverProtocol + DefaultProviderResolver (M2.1.b)
6b76e6d refactor(orchestration): apply M2.1.a code review fixes
cd34835 feat(orchestration): add ExecutionEvent + InvocationReport (M2.1.a)
3a7db29 docs(phase-0/m2): seed Task-detail + Task_history index
+ 14 条 plan 评审 commits（5571291…5cfac70）+ 1 条 main 文档 commit（7155ff7）
```

### Key files（下个 session 必读）

- `docs/Task-detail/2026-04-25-phase-0-m2-orchestration.md` — M2 实施总结（§5 变动文件清单 / §6 测试结果 / §7 修改逻辑 / §7.3 6 个偏离 plan 的合理决策 / §7.6 后续清理项 backlog）
- `docs/superpowers/plans/2026-04-25-phase-0-m2-orchestration.md` — 完整 plan（2232 行 / Task 0-14 + Self-Review checklist + 与 spec §3.4 Step 对照表 + 评审历史）
- `docs/superpowers/specs/2026-04-23-sliceai-v2-roadmap.md` — v2 spec（§3.4 ExecutionEngine / §3.9.6.5 权限闭环 / §3.9.2 §3.9.3 §3.9.5 / §C-1 §C-7 §C-10 各项约束）
- `SliceAIKit/Sources/Orchestration/Engine/ExecutionEngine.swift` + `ExecutionEngine+Steps.swift` — 主流程 actor + FlowContext class + 6 个 step helper（拆 2 文件理由：单文件 runMainFlow 触发 swiftlint function_body 80 行硬限）
- `SliceAIKit/Sources/Orchestration/Permissions/PermissionBroker.swift` + `PermissionBrokerProtocol.swift` + `PermissionGraph.swift` + `EffectivePermissions.swift` — 权限闭环（spec §3.9.6.5 / §3.9.2 下限 / D-24 静态闭环）
- `SliceAIKit/Sources/Orchestration/Telemetry/JSONLAuditLog.swift` + `Redaction.swift` + `CostAccounting.swift` — 遥测 + 脱敏
- `SliceAIKit/Sources/Capabilities/SecurityKit/PathSandbox.swift` + `PathSandboxError.swift` — 安全沙箱（含 macOS `/private/etc` 兜底）
- `SliceAIKit/Sources/Capabilities/MCP/MCPClientProtocol.swift` + `MockMCPClient.swift` — MCP 接口 + production Mock
- `SliceAIKit/Sources/SliceCore/SliceError.swift` + `ContextError.swift` + `ToolPermissionError.swift` — SliceCore 白名单 3 文件（仅这 3 个被改/新增）
- `SliceAIKit/Sources/Orchestration/Executors/PromptExecutor.swift` — V2 PromptExecutor（§C-7 复制非替换的产物；与 v1 `SliceCore/ToolExecutor.swift` 共存到 M3）

## Decisions and rationale

### A. 整体架构原则（M2 期间持守的所有不变量）

1. **§C-1 zero-touch v1**：v1 8 模块（LLMProviders / SelectionCapture / HotkeyManager / DesignSystem / Windowing / Permissions / SettingsUI）+ SliceAIApp 自 baseline `5cdf0f7` 起 **0 行 diff**。app 行为保持 v0.1 + M1 现状不变；M3 才接 AppContainer。
2. **§C-7 PromptExecutor 复制非替换**：`SliceCore/ToolExecutor.swift` 自 baseline 起 **0 行 diff**；新增 `Orchestration/Executors/PromptExecutor.swift` 是 V2 升级版，M3 才删旧文件 + rename pass。
3. **SliceCore 白名单 3 文件**：仅 `SliceError.swift`（Modify，加 `.context` / `.toolPermission` 顶层 case）+ `ContextError.swift`（Create）+ `ToolPermissionError.swift`（Create）。M1 既有 324 个 SliceCoreTests 全绿。
4. **§C-3 / §C-10 不变量**：`PermissionBroker.swift` / `ExecutionEngine.swift` / `ExecutionEngine+Steps.swift` / `InvocationReport.swift` 共 4 个文件 grep `default:` 出现次数 **全部为 0**——所有 enum-switch 都是 exhaustive。
5. **错误模型脱敏**：所有带 String payload 的 case 在 `developerContext` 一律输出 `<redacted>`（SliceError + MCPClientError 同口径；Task 13 polish 才补齐 MCP）。
6. **plan / round 元数据**：源码 + 测试 + Task-detail 全部 grep 残留 = **0 匹配**（`Round X` / `RX-PX` / `B-X.Y` / `评审第N` 等内部 plan-evolution 元数据均已扫除）。

### B. 6 个偏离 plan 的合理决策（详见 Task-detail §7.3）

| 项 | plan | 实施 | 理由 |
|---|---|---|---|
| ExecutionEngine 单文件 vs 拆 2 文件 | plan 给单文件可编译代码块 | 拆 `ExecutionEngine.swift` + `ExecutionEngine+Steps.swift`（含 FlowContext class） | swiftlint function_body 80 行硬限；FlowContext 在 actor-isolated scope 串行使用，无 Sendable 警告 |
| `escapesWhitelist` case 名 | plan / spec 措辞 | 改名为 `escapesAllowlist` | swiftlint `inclusive_language` 默认 enabled 拒绝 `whitelist` |
| MCP/Skill production-side Mock 命名 | plan 暗示 `Mock*` | 沿用 `Mock*`（项目其他 production stub 用 `InMemory*` 风格——延期到 Phase 1 改名） | 改名成本低但跨 Phase 1+ 一并处理更稳 |
| 4 个 concrete actor mock | plan §1658 列了 10 个 Mock | ContextCollector / PermissionGraph / PromptExecutor / CostAccounting 是 actor 不能 mock，沿用"真实 actor + 控制上游 fakes"模式 | Swift 6 actor 不能被 protocol mock；plan 设想与 §C-10.1 audit 表落地的 actor 实施现实有偏差 |
| PathSandbox 硬禁止表 | plan 仅列 `/etc/` | 新增 `/private/etc/` + `/private/var/db/` 兜底 | macOS 系统级 symlink `/etc → /private/etc`；user allowlist `/private/etc/` 加进来即可绕过原 `/etc/` 硬禁止——code-quality reviewer 抓出的真实安全漏洞，已修 + 加金标准回归测试 |
| Token 估算公式 | spec §4.4.2 `count / 4` | 同 + `max(1, …)` 兜底 + `?? .zero` 替代 force unwrap | spec 没说空 prompt 估 0 还是 1；选 max(1, …) 让空 prompt 也算 1 token 避免 CostAccounting 永远写 0；swiftlint --strict 禁 `!` |

### C. 完整 PR 模板（plan §2119 + 实测数字）

下个 session 开 PR 时直接用：

```markdown
## Summary

落地 v2.0 spec §3.4 / §3.9 的执行引擎 + 权限闭环 + 安全模型骨架；新增 ~30 个文件 / ~3000 行；零触及 v1 + M1 v2 类型。

## What's in scope

- Orchestration: ExecutionEngine actor 主流程 Step 1-10 / ContextCollector / PermissionBroker / PermissionGraph / CostAccounting / JSONLAuditLog / OutputDispatcher / PromptExecutor
- Capabilities: PathSandbox / MCPClientProtocol + Mock / SkillRegistryProtocol + Mock

## What's NOT in scope

- AppContainer 接入（M3）
- ToolExecutor 删除（M3）
- 真实 MCPClient（Phase 1）
- 真实 SkillRegistry（Phase 2）
- PresentationMode 非 .window 分支（Phase 2）

## Test plan

- [x] swift test --parallel --enable-code-coverage 全绿（**545 / 545 pass**）
- [x] swiftlint lint --strict 0 violations（133 files）
- [x] Orchestration 覆盖率 89.12%（≥75%）
- [x] Capabilities 覆盖率 97.64%（≥60%）
- [x] SliceCore 覆盖率 92.73%（≥90%）
- [x] zero-touch（v1 既有 + SliceAIApp + ToolExecutor 共 0 行 diff；SliceCore 仅白名单 3 文件）
- [x] xcodebuild Debug build BUILD SUCCEEDED
- [x] enum-switch default 反向断言（PermissionBroker / ExecutionEngine / ExecutionEngine+Steps / InvocationReport 全 0 default）

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

## Next steps (ordered by priority)

### 1. 启动 Codex review loop（用户明确要求的下一步）

调用 `superpowers:codex-review-loop` skill 或类似 Codex review-fix-review 流程，让 Codex 对 worktree 当前状态做反复 adversarial review，直到无 issue 再进 PR。

**Codex review 应聚焦**：

- 本会话的"6 个偏离 plan 的合理决策"是否真合理（特别是 ExecutionEngine 拆分 + FlowContext class 在 Swift 6 严格并发下的安全性 + 是否未来加 step 容易引入 Sendable 警告）
- ExecutionEngine 主流程 Step 7 sideEffects 路径的 `partialFailure` 分支：当 sideEffect[0] approved 触发并写 audit、sideEffect[1] denied 标 partialFailure 时，是否会让用户感知到"看起来成功了但实际部分失败"——UX 层面是否需要更强信号
- §C-7 复制非替换：v1 ToolExecutor 与 V2 PromptExecutor 同时存在期间，是否有"两套 prompt 渲染逻辑漂移"的隐患（特别是 v1 / V2 各自的字符串模板替换是否完全等价）
- PathSandbox 修复后的硬禁止表是否覆盖完整（除 `/etc → /private/etc` + `/var → /private/var`，是否还有 `/tmp → /private/tmp` 之类的盲区？M2 plan 不含 `/tmp` 但本身在 macOS 上是用户写入区——影响范围可能不同）
- MCPClientError.developerContext 的 `.toolNotFound` case 把 `ref.server` + `ref.name` 原样输出——是否在 Phase 1 真实 MCPClient 接入用户自定义 server 场景下会泄露用户配置（如 server name 含项目名 / 路径）
- Task 4 测试矩阵的 7 条 prompt-kind 路径是否覆盖完整：spec §3.4 Step 5 还有 `.agent` / `.pipeline` 走 `finishNotImplementedKind` 的路径是否有专测
- Task 13 production-side Mock 在 Sources/Capabilities/ 而非 Tests target 的安全风险：SwiftPM dead-code-strip 真的会清掉 production binary 里未引用的 Mock 类型吗？（这是 Task 13 code-quality reviewer 的疑问，未深入验证）

**已知 6 个 Minor backlog 项**（Task-detail §7.6 列出，不阻塞合并；Codex 若再次提出可标记"已接受 defer 至 Phase 1+"）：MCPCallResult.meta 类型迁移路径 / Mock 命名 InMemory* / extractContextRequests 重复 / estimateCostUSD magic number / ExecutionEngine+Steps.swift 接近 swiftlint warning 500 / FlowContext 加 invariant 注释。

### 2. Codex review 通过后开 PR

```bash
cd /Users/majiajun/workspace/SliceAI/.worktrees/phase-0-m2
git push -u origin feature/phase-0-m2-orchestration
gh pr create --base main --head feature/phase-0-m2-orchestration \
  --title "Phase 0 M2: Orchestration + Capabilities 骨架" \
  --body "<上面 §C 的 PR 模板>"
```

**完成定义**：PR URL 返回给用户。

### 3. 等 PR review 反馈

PR 通常会触发：① CI 跑 `.github/workflows/ci.yml`（swift build + swift test --parallel + swiftlint --strict） ② reviewer comment。
本地已实测全过，CI 不应有惊喜；reviewer comment 视情况处理。

## Known traps / do not touch

1. **Worktree vs 主仓库 cwd 陷阱**：本 worktree 是 git **subdirectory worktree** —— 根目录是完整仓库（含 `SliceAIApp/` `docs/` `SliceAI.xcodeproj` 等），但 swiftlint 的 `included:` 配置写在 worktree 根的 `.swiftlint.yml`。如果在 `SliceAIKit/` 子目录跑 `swiftlint lint --strict`，swiftlint 会找不到配置 → 扫描所有 `.swift` 文件（含 `.build/` 自动生成 + Tests）→ 报假阳 errors（line_length / identifier_name 等）。**正确做法**：永远 `cd /Users/majiajun/workspace/SliceAI/.worktrees/phase-0-m2 && swiftlint lint --strict`。本会话已踩过一次坑。
2. **SourceKit "No such module" 假阳**：每次新建 / 修改文件后 SourceKit 都报 `No such module 'SliceCore' / 'XCTest' / 'Capabilities'`。原因是 worktree SwiftPM 索引不刷新；忽略它，唯一真值来源是 `swift build` / `swift test` / `swiftlint --strict` 实际退出码。本会话整个过程都被这个假阳骚扰过；不要被它误导去做无效修复。
3. **ExecutionEngine.swift 0 `default:` 兜底是不变量**：plan §2090–2103 + 项目惯例都要求 PermissionBroker / ExecutionEngine 等关键 enum-switch 文件不能用 `default:` 兜底。新增 enum case 时必须同步更新所有 exhaustive switch 站点；CI 用 grep `default:` 反向断言保障。**任何"加个 default 简化代码"的 PR comment 都应该拒绝**。
4. **plan / round 元数据"0 匹配"是项目硬规则**：所有源码 + 测试 + Task-detail 不允许出现 `Round X` / `RX-PX.X` / `B-N.M` / `评审第` / `plan R-` / `M? plan R` 等内部 plan-evolution 元数据。本会话最后清理过一次（commit `2fa006c`）；任何新 PR / commit 引入这类残留必须立即清掉。
5. **§C-1 zero-touch 验证命令路径细节**：以前 zero-touch 验证用 `git diff -- SliceAIKit/Sources/...`（完整路径前缀）—— 必须从 worktree 根跑，否则 git 拼接 cwd 后路径错误，diff 输出 0 行其实是路径不存在。本会话踩过一次这个坑。
6. **MCPClientError.developerContext 测试**：Task 13 polish commit `3feacbc` 加了 `test_mcpClientError_developerContext_redactsStringPayloads`。如果未来给 MCPClientError 加新 case，**必须**同步给 `developerContext` 加新分支（exhaustive switch 编译时强制）+ 在该测试里加新断言。
7. **PathSandbox `/private/etc` 安全 fix 测试**：`test_normalize_userAllowlistCannotBypassHardDeny_throwsEscapesAllowlist` 是 Task 12 安全 fix 的金标准回归测试。任何对 PathSandbox 硬禁止表 / user allowlist 逻辑的改动必须保证这条测试仍 throw `.escapesAllowlist`。
8. **Codex review 路径**：用户要求"反复 review 直到无 issue"。需要警惕"过度工程化建议"——本会话已有 5 个 Minor 项被合理 defer，Codex 若再次提出类似建议（命名 / magic number / 拆文件），应主动援引 Task-detail §7.6 backlog 说明已接受延期。
9. **PR 不能力 force push**：`feature/phase-0-m2-orchestration` 是首次 push，`-u origin` 即可；后续若有改动，常规 `git push` 即可。**不要** `--force` 除非用户明确要求。
10. **xcodebuild 必须在 worktree 根跑**：worktree 内有 `SliceAI.xcodeproj`（subdirectory worktree 包含主仓库全部内容）。**不要** 切到主仓库去跑 xcodebuild —— 主仓库 HEAD 是 main，跑出来跟 worktree 代码无关。

## Required reading (in order)

1. `CLAUDE.md`（项目通用规则）
2. `docs/Task-detail/2026-04-25-phase-0-m2-orchestration.md`（M2 实施总结，§5/§6/§7 含全部数据）
3. `docs/superpowers/plans/2026-04-25-phase-0-m2-orchestration.md`（plan，2232 行；Codex review 时按需局部 grep）
4. `docs/superpowers/specs/2026-04-23-sliceai-v2-roadmap.md`（spec；按需查 §3.4 / §3.9 节）

**不要**默认读全部 plan / spec —— 都是 2000+ 行；按需 grep。

## Minor changes (side work outside the main thread)

无。本 session 所有改动都在 M2 主线（41 个 commits 全部 traceable 到 14 个 Task）。

## 附：完整 CI gate 实测数字（Task 14 验证结果，可直接进 PR body）

| 验证项 | 结果 |
|---|---|
| `swift build` | Build complete (0.20s) |
| `swift test --parallel --enable-code-coverage` | **545 / 545 pass** (0.48s) |
| `swiftlint lint --strict` | 133 files / **0 violations / 0 serious** |
| `xcodebuild -project SliceAI.xcodeproj -scheme SliceAI -configuration Debug build` | **BUILD SUCCEEDED** |
| Orchestration 覆盖率（excl Tests/Mock） | **89.12%** |
| Capabilities 覆盖率（excl Mock） | **97.64%** |
| Capabilities 覆盖率（incl Mock） | 98.08% |
| SliceCore 覆盖率 | **92.73%** |
| v1 8 模块 + SliceAIApp + ToolExecutor.swift git diff | 0 行 |
| SliceCore 实际改动文件 | 仅 SliceError.swift / ContextError.swift / ToolPermissionError.swift（3 白名单） |
| PermissionBroker / ExecutionEngine / +Steps / InvocationReport `default:` count | 全 0 |
| plan / round 元数据残留 grep | 0 匹配 |
| M1 既有 SliceCoreTests | 324 / 324 pass（baseline 是 324，M1 V2 类型测试已含其中） |
