{
  "findings": [
    {
      "severity": "medium",
      "title": "MCPCallResult.meta encodes/decodes as JSON key \"meta\" instead of MCP wire \"_meta\"",
      "file": "SliceAIKit/Sources/SliceCore/MCPContentItem.swift",
      "line_start": 145,
      "line_end": 163,
      "confidence": 0.85,
      "evidence": "MCPCallResult is declared as `public struct ... Codable` with stored property `public let meta: MCPJSONValue.Object?` and no custom CodingKeys / init(from:) / encode(to:). Swift's synthesized Codable maps the property name verbatim to JSON key \"meta\". MCP 2025-06-18 (and the MCP TypeScript SDK's BaseResultSchema) reserves \"_meta\" — with leading underscore — for protocol metadata on result objects, and the task itself cites that spec (`docs/Task-detail/2026-05-06-phase-1-m1-task-1-mcp-json-contract.md` lines 13–15) and applies wire shape rigorously elsewhere in the same file (MCPContentItem's golden-JSON tests in `Tests/SliceCoreTests/MCPContentItemTests.swift` lines 30–35, 47–61 verify exact wire keys `type` / `text` / `mimeType` / `resource_link` / `resource`). The CallResult round-trip tests at `Tests/CapabilitiesTests/MCPClientProtocolTests.swift` lines 107–130 only encode-then-decode the same Swift struct, so the divergence from MCP wire is not caught. When Task 2's real stdio/Streamable HTTP client decodes a CallToolResult with `\"_meta\": {...}` directly into MCPCallResult via Codable, the `meta` field will silently be nil and protocol metadata is lost; encoding the Swift value to wire JSON likewise emits `\"meta\"` rather than `\"_meta\"`. This breaks the contract's stated wire-shape discipline (`docs/Module/SliceCore.md` describes MCPCallResult as the MCP tool-call result contract alongside MCPContentItem, which is wire-shape).",
      "recommendation": "Either (a) add an explicit CodingKeys mapping `case meta = \"_meta\"` to MCPCallResult so its Codable matches MCP 2025-06-18 wire shape (trivial, mirrors MCPContentItem's wire discipline) and add a golden-JSON test asserting the encoded key is `\"_meta\"`; or (b) document explicitly in `docs/Module/SliceCore.md` and the type's docstring that MCPCallResult is a domain-shape (not wire-shape) value type and that Task 2's client must translate `_meta`↔`meta` at the wire boundary, then add a comment in MCPClientProtocol.swift pointing to that translation contract. Option (a) is simpler and consistent with MCPContentItem's design; KISS-wise it removes a hidden translation step that Task 2 would otherwise have to add."
    }
  ],
  "summary": "Reviewed Task 1's SliceCore MCP JSON/value contract (MCPJSONValue, MCPContentItem, MCPCallResult, MCPToolDescriptor, MCPTransport.streamableHTTP, OutputBinding/PipelineStep param uplift, audit-log key-aware recursive scrubbing). The transparent-raw-JSON Codable, content-item discriminator + nested-resource wire shape + text/blob union validation, transport Phase-1 gate, and audit redaction all hold up under scrutiny. Material concern: MCPCallResult's `meta` field uses synthesized Codable mapping to JSON `\\\"meta\\\"` while MCP 2025-06-18 spec reserves `\\\"_meta\\\"` for result metadata. This contradicts the wire-shape discipline applied in the same file's MCPContentItem (which has golden-JSON tests proving exact MCP wire keys), and the existing self-roundtrip tests don't catch it. When Task 2 lands the real MCP client, this will silently drop server-emitted `_meta` on decode and emit a non-spec field on encode. Recommend adding `case meta = \\\"_meta\\\"` CodingKeys (or explicitly documenting CallResult as domain-shape with a translation boundary in Task 2). Audio content type omission is intentional per spec §4.2 scope; no other material issues found.",
  "verdict": "needs_attention"
}
